//! Diagnostics desktop state re-exported from the shared diagnostics type split.

pub use crate::diagnostics_types::{
    DiagnosticsState, DiagnosticsTab, MissingCoverage, ProofBlocker, RecoveryAnchor, TraceChain,
};
