# Libraries

## External Library Baseline

| Library | Role in `engine_storage_mutation` |
|---|---|
| `smallvec` | Low-allocation mutation buffers and batched payload support. |
| `bitflags` | Compact mutation and apply flags. |

## Fit

Each listed dependency serves the primary role of `engine_storage_mutation` and stays aligned to its pressure axis.
