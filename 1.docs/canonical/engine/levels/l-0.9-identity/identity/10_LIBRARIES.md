# Libraries

## External Library Baseline

| Library | Role in `engine_identity` |
|---|---|
| `slotmap` | Stable identity key substrate. |
| `serde` | Serialization for identity types. |

## Fit

Each listed dependency serves the primary role of `engine_identity` and stays aligned to its pressure axis.
