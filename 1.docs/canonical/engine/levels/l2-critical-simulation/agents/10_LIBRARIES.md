# Libraries

## External Library Baseline

| Library | Role in `engine_agents` |
|---|---|
| `glam` | Spatial reasoning for navigation and perception. |
| `serde` | Serialization for agent descriptors and outputs. |
| `bitflags` | Compact state and policy flags. |
| `smallvec` | Low-allocation batch metadata. |
| `tracing` | Family diagnostics. |

## Fit

Each listed dependency serves the primary role of `engine_agents` and stays aligned to its pressure axis.
