# Ingest

## Role

Content ingest.

## Canonical Definition

`Ingest` is a canonical element of `engine_content` inside `L3.2. Resource Systems`. It exists as one explicit part of the engine stack and does not transfer its ownership class into another crate or level.

## Data Model

Resource intake structures and ingest boundaries for external content entering engine pipelines.

## Dependencies

This element depends on the canonical lower boundaries required by `engine_content`:

- `engine_core`

## Layer Links

- parent crate: `engine_content`
- level: `L3.2. Resource Systems`
- layer document: `00_LAYER.md`
- libraries: `10_LIBRARIES.md`
- dependencies: `20_DEPENDENCIES.md`
- communication: `30_COMMUNICATION.md`
- threading: `31_THREADING.md`
