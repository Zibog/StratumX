# Threading

## Ownership

Uses runtime threading law; no separate global threading ownership.

## Posture

Interactive execution over the shared runtime constitution.

## Parallel Units

Same runtime partitions as engine_runtime, tuned for interactive cadence.

## Write Model

World writes remain runtime-governed staged apply.
