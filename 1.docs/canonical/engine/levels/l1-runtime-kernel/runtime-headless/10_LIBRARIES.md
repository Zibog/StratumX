# Libraries

## External Library Baseline

| Library | Role in `engine_runtime_headless` |
|---|---|
| `tracing` | Profile-level diagnostics. |
| `smallvec` | Low-allocation profile-local descriptors. |

## Fit

Each listed dependency serves the primary role of `engine_runtime_headless` and stays aligned to its pressure axis.
