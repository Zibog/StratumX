# Libraries

## External Library Baseline

| Library | Role in `engine_core` |
|---|---|
| `glam` | Unified engine math backbone. |
| `serde` | Serialization for shared types and contracts. |
| `thiserror` | Typed error model. |

## Fit

Each listed dependency serves the primary role of `engine_core` and stays aligned to its pressure axis.
