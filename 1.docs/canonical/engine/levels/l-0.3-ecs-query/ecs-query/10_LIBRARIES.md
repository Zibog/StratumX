# Libraries

## External Library Baseline

| Library | Role in `engine_ecs_query` |
|---|---|
| `smallvec` | Low-allocation query descriptor support. |
| `bitflags` | Compact query and access flags. |

## Fit

Each listed dependency serves the primary role of `engine_ecs_query` and stays aligned to its pressure axis.
