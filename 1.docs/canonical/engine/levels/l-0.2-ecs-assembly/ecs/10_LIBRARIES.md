# Libraries

## External Library Baseline

| Library | Role in `engine_ecs` |
|---|---|
| `serde` | Serialization where ECS substrate descriptors require it. |

## Fit

Each listed dependency serves the primary role of `engine_ecs` and stays aligned to its pressure axis.
