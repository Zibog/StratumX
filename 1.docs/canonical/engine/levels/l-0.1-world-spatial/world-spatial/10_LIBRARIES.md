# Libraries

## External Library Baseline

| Library | Role in `engine_world_spatial` |
|---|---|
| `glam` | World-space math. |
| `serde` | Serialization for spatial structures. |

## Fit

Each listed dependency serves the primary role of `engine_world_spatial` and stays aligned to its pressure axis.
