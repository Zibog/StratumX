# Libraries

## External Library Baseline

| Library | Role in `engine_ecs_registry` |
|---|---|
| `serde` | Serialization for structural registry descriptors where required. |

## Fit

Each listed dependency serves the primary role of `engine_ecs_registry` and stays aligned to its pressure axis.
