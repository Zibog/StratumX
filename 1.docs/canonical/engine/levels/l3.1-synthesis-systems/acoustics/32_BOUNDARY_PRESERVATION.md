# Boundary Preservation

## Widened Replacement

`engine_acoustics` replaces the earlier standalone `engine_audio` boundary.

## Canonical Meaning

Audio-facing concerns are preserved, but the boundary is widened to model spatial and material-aware acoustic synthesis rather than simple audio handling.

## Reason

The master canon treats sound as an acoustic synthesis family rather than a flat output system.
