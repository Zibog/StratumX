# Libraries

## External Library Baseline

| Library | Role in `engine_world` |
|---|---|
| `serde` | Serialization for world state and read structures. |
| `bincode` | Binary baseline for snapshots and world-state transfer. |
| `glam` | Spatial support inside world-bound state where required. |

## Fit

Each listed dependency serves the primary role of `engine_world` and stays aligned to its pressure axis.
