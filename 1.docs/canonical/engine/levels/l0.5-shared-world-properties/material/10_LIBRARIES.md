# Libraries

## External Library Baseline

| Library | Role in `engine_material` |
|---|---|
| `serde` | Serialization for material descriptors and profiles. |
| `bitflags` | Compact material state and response flags. |
| `smallvec` | Low-allocation property and response tables. |

## Fit

Each listed dependency serves the primary role of `engine_material` and stays aligned to its pressure axis.
