# Libraries

## External Library Baseline

| Library | Role in `engine_handle` |
|---|---|
| `slotmap` | Stable keyed indirection where justified. |
| `serde` | Serialization for handle types. |

## Fit

Each listed dependency serves the primary role of `engine_handle` and stays aligned to its pressure axis.
