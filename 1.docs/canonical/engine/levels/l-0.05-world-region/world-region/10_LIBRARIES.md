# Libraries

## External Library Baseline

| Library | Role in `engine_world_region` |
|---|---|
| `serde` | Serialization for region descriptors. |
| `bitflags` | Compact region state flags. |

## Fit

Each listed dependency serves the primary role of `engine_world_region` and stays aligned to its pressure axis.
