# 85_PROJECT_TO_WORLD_AUTHORING_HANDOFF_CANON

**Stack version:** `SX-CANON/1.0.24/STACK-v30`

Project handoff becomes lawful only when the path is explicit:

project bootstrap → content import → package/layer bind → world authoring → gameplay authoring → heavy domain authoring

The handoff must declare:

- owning button ids
- owning route ids
- owning labs
- first focus after success
- first focus after failure
- required artifacts
