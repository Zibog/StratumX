# Build Validation Release Family

Canonical family: `build_validation_release_family`.

## Owns
- build/validation/release suite aggregation only
- build runtime
- release runtime
- validation runtime
- artifact and dependency projections

## Family law
`build_validation_release_family` exists to describe editor-local composition, not to replace the member level contracts.
