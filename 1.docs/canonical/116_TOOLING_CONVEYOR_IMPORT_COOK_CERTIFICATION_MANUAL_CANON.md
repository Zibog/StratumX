# Tooling Conveyor Import Cook Certification Manual Canon

**Stack version:** `SX-CANON/1.0.28/STACK-v34`

## Purpose
Provide the stitched reading spine for import, canonicalization, cook, benchmark, compare, proof, and release-seal routing so the tooling layer reads as one conveyor rather than a bag of routes.

## Scope of this manual
This manual stitches:
- root `108–109`, `116`;
- tooling `83–92`;
- sdk `79–84`, `88`;
- editor `107`, `119–127`, `124`, `126`, `135–136`.

## Reading order
1. root asset and workload law `108–109`;
2. tooling `83–89` for import/canonicalization and family blockers;
3. tooling `90–91` for benchmark and golden diff execution;
4. tooling `87–88` and `92` for proof-lane, release seal, and focus recovery;
5. editor and sdk companions for operator surfaces and packet consumers.

## Mandatory exact outcomes
- import and canonicalization do not fabricate or erase lineage;
- compare and golden diff routes are exact and reviewable;
- proof-lane routing is narrow and honest;
- release-seal routing cannot skip blocker context;
- recovery routes always return to the narrowest useful operator surface.

## Current posture
`document_gold / stitched_manual_closed`
