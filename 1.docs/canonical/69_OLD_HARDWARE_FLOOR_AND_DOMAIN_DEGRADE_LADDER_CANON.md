# Old Hardware Floor And Domain Degrade Ladder Canon

**Stack version:** `SX-CANON/1.0.24/STACK-v30`

## Purpose
Define ruthless degrade ladders for the declared 10-year-old hardware floor.
The goal is sustained legal operation without truth fraud.

## Global floor targets
- frame target: `60 fps` nominal, `>= 50 fps` transient recovery window only;
- CPU red ceiling: `16.6 ms sustained`;
- GPU red ceiling: `16.6 ms sustained`;
- RAM ceiling: `6.0 GiB` resident working set;
- disk hitch ceiling: `16 ms` frame-visible stall forbidden.

## Exact domain degrade order
| Domain | Step 1 | Step 2 | Step 3 | Step 4 | Step 5 | Forbidden shortcut |
|---|---|---|---|---|---|---|
| large world / streaming | shrink far residency radius | lower distant transition frequency | reduce non-critical visibility debug density | compact route history mirrors | sampled non-authoritative overlays only | hiding residency gaps while claiming stable streaming |
| hydrology | reduce far-field update cadence | reduce inactive container fidelity | compact history window | disable non-critical compare overlays | sampled puddle presentation only outside active bubble | falsifying fill, leak, or restore truth |
| fire / weather / smoke | reduce volumetric march density | reduce cloud self-shadow refresh | reduce distant turbulence detail | clamp post-weather composites | sampled debug overlays only | replacing storm or smoke truth with skybox or post-only lie |
| cloth / fur | reduce solver density | reduce far contact detail | reduce secondary motion horizon | reduce non-critical wetness visualization density | sampled preview for non-authoritative surfaces | deleting contact truth while claiming active solve |
| population / tactics | reduce far-agent decision cadence | reduce non-critical social diagnostics | reduce squad overlay density | lower compare frequency outside active bubble | compact retained history mirrors | skipping legality of suppression, flank, or needs inside active bubble |
| ecology | reduce distant migration solve cadence | bucket low-priority prey updates | compact route history | sampled route overlay mode | sampled predation visualization only | teleporting migration truth without declared code |
| wounds / ballistics | reduce non-critical debug detail | compact history and compare payloads | lower retained artifact mirror count | sampled presentation only | sampled gore presentation only | falsifying lethality or penetration truth |
| semantic runtime | reduce deep explain payload size | compact compare payloads | lower retained conversation mirror count | sampled visualization only | sampled long-tail explain branches only | inventing or hiding world consequence |
| presentation/runtime | reduce pass density, volumetric density, and transient detail in the declared per-family order | compact compare payloads | reduce non-critical overlays | clamp sampled debug text density | retain first blocker and rung only | pretending that a higher-quality frame or sound path survived when it did not |

## Regression obligations
- every old-floor certification must be rerunnable from `editor/81`;
- every mixed old-floor result must preserve the actual rung taken, not only the final verdict;
- every regression run must point to one canonical pack in root `81`;
- every freeze-relevant pack must carry one old-floor result or a typed exemption verdict;
- silence is forbidden: if no old-floor result exists, signoff remains blocked.

## Mixed-pack floor law
The mixed floor posture for the brutal proof-region is legal only when:
- world tiering, streaming, residency, presentation, audio, and persistence each publish their actual rung;
- compare digests remain attached to the same retained bundle;
- no domain claims green while another domain secretly forced an undeclared shortcut.

## Law
Degrade may reduce cost.
It may not delete causality, baseline lineage, blocker visibility, or first-failure publication.
