# Tooling Evidence Authority Transaction v4

## Purpose
This active evidence artifact records a granular closure slice of the package.

## Verified coverage
- traces `authority_core`, `command_envelopes`, and `transaction_ledger` ownership and outputs
- confirms all editor mutations lower through command and transaction lanes only
- confirms forbidden direct-apply and hidden-truth paths remain denied
- maps command schema, transaction outputs, and replay legality to active docs

## Status
active
