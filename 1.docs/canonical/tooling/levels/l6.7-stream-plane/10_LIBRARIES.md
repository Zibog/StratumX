# Libraries

This contract belongs specifically to the stream plane level and may not be reused verbatim by another tooling level.


## Local schemas and ids
- `StreamEventId` schema for `stream_event_id`
- `StreamKind` schema for `stream_kind`
- `SourceScopeId` schema for `source_scope_id`
- `StreamOrderingCursor` schema for `ordering_cursor`
- `RetentionWindow` schema for `retention_window`

## Declared registry use
- resolves or references `l6.0-authority-core`
- resolves or references `l6.9-budget-runtime`
