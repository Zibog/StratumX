# tool_preview_requests communication

Ingress:
- viewport preview refresh
- asset/browser preview request
- manual preview regenerate actions

Egress:
- preview request envelopes keyed by preview_request_id
- cancel and supersede notifications
- quality-hint publications

Communication law:
the sidecar is append-only publication traffic, never hidden authority mutation.
