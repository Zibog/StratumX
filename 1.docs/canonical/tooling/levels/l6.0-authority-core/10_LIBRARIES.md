# Libraries

This contract belongs specifically to the authority core level and may not be reused verbatim by another tooling level.


## Local schemas and ids
- `AuthorityEpoch` schema for `authority_epoch`
- `WriterScopeId` schema for `writer_scope_id`
- `MutationTicket` schema for `mutation_ticket`
- `DenyReasonCode` schema for `deny_reason`
- `AuthoritySnapshotRef` schema for `authority_snapshot_ref`

## Declared registry use
- resolves or references `l6.2-transaction-ledger`
- resolves or references `l6.9-budget-runtime`
