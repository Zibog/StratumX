# Libraries

This contract belongs specifically to the budget runtime level and may not be reused verbatim by another tooling level.


## Local schemas and ids
- `BudgetScopeId` schema for `budget_scope_id`
- `ResourceClass` schema for `resource_class`
- `HardLimit` schema for `hard_limit`
- `SoftLimit` schema for `soft_limit`
- `PressureState` schema for `pressure_state`

## Declared registry use
- resolves or references `l6.8-cache-plane`
