# Family Local Communication

This local family contract belongs specifically to the l6.f0 workspace shell family and may not be reused by a different family key.


## Coordinates for `workspace_shell_family`
- routing, locality, or refresh updates around workspace shell composition, layout sessions, focus/selection presentation, and workspace host routing
- routing, locality, or refresh updates around authority-facing minimal truth: workspace session ownership only
- routing, locality, or refresh updates around snapshot classes: workspace snapshots and session-scoped view snapshots
- routing, locality, or refresh updates around index classes: workspace/session lookup indices
- routing, locality, or refresh updates around derived classes: derived workspace panels and inspector views

## Operational note
This file remains active and package-specific for `workspace_shell_family` / `30_COMMUNICATION.md`.

## Scope note
The authority, dependency, and audit meaning of 30 COMMUNICATION is defined by the surrounding package and may not be inferred from another file with a similar filename.

## Review use
A reviewer should be able to use this document as part of an implementation checklist, not as decorative filler.
