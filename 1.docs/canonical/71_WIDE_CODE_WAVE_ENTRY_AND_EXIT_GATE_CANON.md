# Wide Code Wave Entry And Exit Gate Canon

**Stack version:** `SX-CANON/1.0.24/STACK-v30`

## Purpose
State when broad implementation may start, what each implementation phase must close, and when the archive may honestly claim wide code-wave readiness.

## Phase gates
| phase | phase entry gate | phase exit gate |
|---|---|---|
| 1 — command spine | `editor/110` is authoritative; active contour uses only `btn.*`; root `74`, `75`, sdk `77`, and editor `100` speak one lifecycle language | every promoted `btn.*` row is traceable as `editor -> tooling -> sdk -> engine -> diagnostics/capture`; root `77–80` are implemented as live checks; no command is handled silently |
| 2 — material world | phase 1 exit gate is satisfied; `editor/58`, `59`, `66`, `67`, and `110` expose promoted phase-2 buttons; root `81` contains the phase-2 packs | mud vs asphalt, cascade collapse, hydrology continuity, fire-weather-smoke coupling, and far-storm visibility are runnable from editor, comparable against baseline, recoverable, and evidence-bearing |
| 3 — live world | phase 2 exit gate is satisfied; editor `69–80` expose production surfaces for society, tactics, ecology, wounds, semantic, nav, persistence, inventory, quest, and causality; root `76` has one signal row per live-world phenomenon | need-pressure crime shift, tactics replanning after cover break, migration reroute, navigation rebuild, inventory scarcity reaction, quest delayed consequence, guarded semantic outcome, and why-debugger explanation all round-trip through compare/capture/recover/certify without hidden authority |
| 4 — presentation/runtime | phase 3 exit gate is satisfied; editor `90–98` expose production surfaces; root `66`, `72`, `75`, `76`, `81` each contain phase-4 rows with no compare/capture naming drift | framegraph legality, material truth, residency pressure, tunnel flash, storm visibility, audio timing, runtime contact solve, VFX, and UI all round-trip through compare/capture/recover/certify with old-floor posture visible |
| 5 — brutal proof-region | phases 1–4 exit gates are satisfied; product relay files `24–31`, `34–39`, `70`, `73` are aligned; root `81` registers `pack.brutal_proof_region_relay`; editor `82`, `86`, `103`, `105`, `106`, `107`, `108`, `109`, `110` speak one relay vocabulary | one brutal proof-region is authored, saved, restored, replay-compared, certified, frozen, built, exported, launched, and first-result verified through editor-only routes; mixed packs and old-floor results remain attached to the same retained bundle |
| 6 — certification and scale | phase 5 exit gate is satisfied; regression, freeze, and scale documents speak one pack and baseline vocabulary; editor `81`, `87`, `89`, `103`, `105`, `109` form one review contour | proof-region is freeze-safe in docs, regression-safe in docs, and forms the lawful seed for region-scale expansion without hidden shortcut or undocumented fallback; every freeze blocker, waiver, and exemption resolves to one retained bundle chain |

## Honest readiness statement
This archive may claim **wide code-wave documentation readiness**.
It may not claim **wide code-wave runtime readiness** until the phase exit gates are backed by live code and measured evidence.
