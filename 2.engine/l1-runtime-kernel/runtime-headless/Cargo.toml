[package]
name = "engine_runtime_headless"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Headless runtime variant for server/simulation"
repository = "https://github.com/stratumx/stratumx"
keywords = ["runtime", "headless", "stratumx"]
categories = ["game-development", "network-programming"]

[dependencies]
engine_runtime = { path = "../runtime" }
engine_world = { path = "../../l0-world-truth" }
engine_ecs = { path = "../../l-0.2-ecs-assembly" }
engine_core = { path = "../../l-1-foundation" }
serde.workspace = true

[lib]
path = "src/engine_runtime_headless.rs"
