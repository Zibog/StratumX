#[test]
fn identity_entity_issue_live_0() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_1() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_2() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_3() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_4() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_5() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_6() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_7() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_8() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_9() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_10() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_11() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_12() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_13() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_14() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_15() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_16() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_17() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_18() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_entity_issue_live_19() {
    let mut a = IdentityAllocator::new(IdentityDomain::Entity);
    let id = a.issue_entity().unwrap();
    assert!(a.is_live_entity(id));
}
#[test]
fn identity_component_issue_live_0() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_1() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_2() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_3() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_4() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_5() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_6() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_7() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_8() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
#[test]
fn identity_component_issue_live_9() {
    let mut a = IdentityAllocator::new(IdentityDomain::Component);
    let id = a.issue_component().unwrap();
    assert!(a.is_live_component(id));
}
