#[test]
fn material_lookup_registered_0() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_1() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_2() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_3() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_4() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_5() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_6() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_7() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_8() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_9() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_10() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_11() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_12() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_13() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_14() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_15() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_16() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_17() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_18() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_19() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_20() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_21() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_22() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_23() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_24() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
