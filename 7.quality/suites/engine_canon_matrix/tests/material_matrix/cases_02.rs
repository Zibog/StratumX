#[test]
fn material_lookup_registered_25() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_26() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_27() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_28() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_registered_29() {
    let m = materials();
    let out = m.lookup(MaterialId(1));
    assert!(!out.used_fallback);
    assert_eq!(out.reaction.response_profile, ResponseProfileId(1));
}
#[test]
fn material_lookup_fallback_0() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_1() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_2() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_3() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_4() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_5() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_6() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_7() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_8() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_9() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_10() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_11() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_12() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_13() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_14() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_15() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_16() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_17() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_18() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
#[test]
fn material_lookup_fallback_19() {
    let m = materials();
    let out = m.lookup(MaterialId(99));
    assert!(out.used_fallback);
    assert_eq!(out.descriptor.material_id, MaterialId(0));
}
