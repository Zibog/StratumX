# SUITE ROLE MAP

- `engine_canon_matrix` — canonical engine surface verification using real `2.engine/*` crates.
- `engine_perf_harness` — benchmark harness using real `2.engine/*` crates.
- `sdk_canon_matrix` — sdk bridge behavior tests through `stratumx_test_support`.
- `tooling_canon_matrix` — tooling authoring runtime tests through `stratumx_test_support`.
- `editor_canon_matrix` — editor product tests through `stratumx_test_support`.
- `engine_sdk_link_matrix` — engine/runtime profile boot + sdk bridge linking.
- `sdk_tooling_link_matrix` — sdk/tooling integration path.
